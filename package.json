{
  "name": "semver-action",
  "version": "1.0.1",
  "description": "Checks SemVer version numbers and provides outputs for the various parts and next version numbers",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest --run",
    "build": "tsc",
    "package": "npm run build && ncc build lib/index.js -o dist"
  },
  "keywords": [],
  "author": "step-security",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/step-security/semver-data-action.git"
  },
  "dependencies": {
    "@actions/core": "^2.0.1",
    "axios": "^1.15.2",
    "semver": "^7.8.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3",
    "vitest": "^4.1.5"
  }
}
